name: Deploy Mandanga Platform

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Desplegar en VPS vía SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            set -e
            PROJECT_DIR="/var/www/mandanga-platform"
            cd $PROJECT_DIR

            echo "======================================"
            echo "PASO 1: BACKUP DEL .ENV"
            echo "======================================"
            if [ -f ".env" ]; then
              cp .env /tmp/.env.backup
              echo "✅ .env respaldado en /tmp/.env.backup"
            else
              echo "⚠️  No existe .env — crea uno manualmente: cp .env.example .env && nano .env"
              exit 1
            fi

            echo "======================================"
            echo "PASO 2: SINCRONIZAR CON GITHUB"
            echo "======================================"
            git fetch origin main
            git reset --hard origin/main
            git clean -fd
            echo "✅ Código sincronizado con origin/main"

            echo "======================================"
            echo "PASO 3: RESTAURAR .ENV"
            echo "======================================"
            cp /tmp/.env.backup .env
            echo "✅ .env restaurado correctamente"

            echo "======================================"
            echo "PASO 4: INSTALAR DEPENDENCIAS"
            echo "======================================"
            composer install --no-dev --optimize-autoloader --no-interaction
            echo "✅ Dependencias instaladas"

            # ╔════════════════════════════════════════════════════════════╗
            # ║  ⚠️  BLOQUE TEMPORAL — RECREAR BD DESDE CERO               ║
            # ║  Elimina todos los schemas y recrea la BD con seeders.     ║
            # ║  DESHABILITAR en producción estable: comentar el bloque    ║
            # ║  temporal y descomentar el bloque producción.              ║
            # ╚════════════════════════════════════════════════════════════╝
            echo "======================================"
            echo "PASO 5: MIGRACIONES Y SEEDERS (TEMPORAL)"
            echo "======================================"
            # Limpiar cache de config para que tome el search_path actualizado
            php artisan config:clear

            # --- INICIO BLOQUE TEMPORAL (deshabilitar cuando la BD sea estable) ---
            # Eliminar todos los schemas de PostgreSQL (CASCADE borra tablas, FK, etc.)
            php artisan tinker --execute="
              \DB::statement('DROP SCHEMA IF EXISTS auditoria CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS horario CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS notificacion CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS mensajeria CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS observador CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS evaluacion CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS inscripcion CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS academico CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS core CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS auth CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS ref CASCADE');
              \DB::statement('DROP SCHEMA IF EXISTS public CASCADE');
              \DB::statement('CREATE SCHEMA public');
            "
            echo "✅ Todos los schemas eliminados"
            php artisan migrate --force --seed
            echo "✅ BD recreada desde cero con migraciones y seeders"
            # --- FIN BLOQUE TEMPORAL ---

            # --- INICIO BLOQUE PRODUCCIÓN (descomentar cuando la BD sea estable) ---
            # php artisan migrate --force
            # echo "✅ Migraciones incrementales ejecutadas"
            # --- FIN BLOQUE PRODUCCIÓN ---

            echo "======================================"
            echo "PASO 6: CACHE DE PRODUCCIÓN"
            echo "======================================"
            php artisan config:clear
            php artisan route:clear
            php artisan view:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            echo "✅ Caches generados"

            echo "======================================"
            echo "PASO 7: REINICIAR WORKERS DE COLA"
            echo "======================================"
            php artisan queue:restart
            echo "✅ Signal enviado a workers de cola"
            # Descomenta según tu servidor:
            # sudo systemctl restart php8.2-fpm
            # sudo supervisorctl restart all

            echo "======================================"
            echo "PASO 8: VERIFICACIÓN DE SALUD"
            echo "======================================"
            APP_URL=$(grep "^APP_URL=" .env | cut -d'=' -f2 | tr -d '\r')
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${APP_URL}/api/ping" || echo "000")
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ API respondiendo correctamente (HTTP 200)"
            else
              echo "⚠️  API retornó HTTP ${HTTP_STATUS} — revisar: tail -n 50 storage/logs/laravel.log"
            fi

            echo ""
            echo "🚀 Deploy completado exitosamente."
